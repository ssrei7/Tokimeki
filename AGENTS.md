# 项目铁律（不可违反）

本文件是硬约束。与其他文档或用户临时指令冲突时，先指出冲突并等待确认，不要静默违反。

## 1. 状态所有权
- 确定性内核（代码）持有一切事实：时间、位置、数值、flags、物品、话题状态、NPC 日程、关系轴。
- AI 只产出两样东西：叙述文字，以及 ops 提议。
- AI 输出的任何数值、坐标、时间、状态一律视为不可信，必须经白名单校验后由内核写入。
- 禁止依赖 AI "记住"状态并在下一轮把它当作事实来源。

## 2. 数值泛化
- 禁止硬编码业务数值字段（例如 `affection: number`）。
- 所有数值走 `stats: Record<string, number>`，所有布尔走 `flags: Record<string, boolean>`。
- 条件判断走安全表达式求值器（expr-eval 一类）。禁止 `eval` 与 `new Function`。

## 3. 扩展方式（唯一允许的四条路）
新增玩法只能通过以下方式接入，禁止为单个功能改写核心循环：
1. 注册新 op handler
2. 订阅事件钩子：`onDayStart` / `onEnterNode` / `onEncounter` / `onTimeAdvance` / `onDialogueEnd` / `onOpsApply` / `onDaySettle` / `beforePromptAssemble`
3. 注册新 prompt block（带优先级与 token 预算）
4. 数据包（事件包 / 世界包 / 角色包）

## 4. 存档兼容
- 每份存档带 `schemaVersion`。
- 任何 schema 变更必须在同一次提交内附带 migration 函数并递增版本号。
- 老存档必须能读。无法自动迁移时给出明确提示与手动修复入口，禁止静默丢数据。

## 5. 成本与延迟
- 默认不调 API。以下操作必须是纯本地、零延迟：移动、查看地图、翻手机、看物品栏/日记/结算、点击已生成的话题、读取已有内容。
- 每新增一处 API 调用，必须说明调用频率与合并可能性。
- 禁止"每个 NPC 每 tick 各调一次"这类 N×M 调用模式。

## 6. 交互原则
- 相遇与对话不消耗行动点。玩家为"去哪里"付费，不为"是否开口"付费。
- 时段用尽不是失败状态，只是机会成本。
- 深度剧情必须到场触发；远程终端只能维持关系，不能推进关系。

## 7. 技术栈约束
- 纯前端静态站点。无后端、无账号系统、无服务端存储。
- API key 与全部数据只存在用户浏览器本地（IndexedDB）。
- 不得将用户代码、key、存档内容发往任何非用户显式配置的端点。
- 栈：React + TypeScript + Vite / Zustand / Dexie / zod / expr-eval / Tailwind + CSS 变量 / JSZip / SVG 渲染地图。引入新依赖需先说明理由并锁定版本。
- `core/` 必须可在 Node 环境中运行：不依赖 DOM、不依赖 IndexedDB、不发起网络请求。持久化只存在于 `data/`，AI 调用只存在于 `providers/`。

## 8. 视觉资产
- 存档只存资产 id 或外链 URL，二进制存 IndexedDB，导出打包进 zip。
- 禁止把 base64 图片写进角色/存档 JSON。
- 立绘为单张、可更换、纯可选。缺图必须走回退链降级，不得报错或留白。

## 9. 开发纪律
- 一次一个垂直切片，跑通再扩规模。禁止同时铺开多个半成品系统。
- 每次改动后必须跑通构建；有测试则跑测试。无法验证要明说原因。
- 阶段 4 之前不做界面美化，只保证结构清晰、CSS 变量与组件插槽留对。
- 只做当前阶段要求的事。新想法写进 `docs/ideas.md`，不当场实现。

## 10. 冲突与越界
- 需要修改核心循环、破坏 schema、或偏离本文件时：停下，说明原因和影响面，等确认。
