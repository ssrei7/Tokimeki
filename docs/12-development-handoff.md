# 阶段 10 后续开发交接

更新时间：2026-09-21

本文用于在新的 Codex 窗口中快速继续开发，避免重复通读全部历史文档。它不能替代根目录 `AGENTS.md`；任何新窗口仍必须完整阅读并遵守 `AGENTS.md`。

---

## 1. 新窗口最短启动流程

1. 完整阅读根目录 `AGENTS.md`。
2. 完整阅读本文。
3. 根据当前任务，只阅读本文列出的相关文档章节，不必默认通读所有历史决策。
4. 执行 `git status --short`、`git log -5 --oneline`，确认没有覆盖用户修改。
5. 核对 `src/data/schema/save.ts` 中的 `CURRENT_SCHEMA_VERSION`。
6. 每次只开始一个垂直切片；先报告范围、数据边界、API 调用和是否需要 schema migration。

推荐的新窗口开场指令：

```text
请在 D:\CodexProjects\tokimeki 继续开发。先完整阅读根目录 AGENTS.md 和 docs/12-development-handoff.md，再检查 git status、最近提交和当前 SaveFile schema。只按交接文档的链接补读与本次任务直接相关的章节，不必重新通读全部历史文档。保留所有现有修改和用户资源。一次只做一个垂直切片，完成后运行 build、全量测试、git diff --check，创建单独本地 commit，不 push，然后停止并报告剩余顺序。
```

---

## 2. 当前稳定基线

- 产品名：小小地图。
- 阶段 10 代码侧验收已通过，记录见 `docs/03-roadmap.md` 的“阶段 10 · 有生态”。
- 当前世界 SaveFile schema：v44。v42 将 StoryScene 地点、日期、时段改为可选历史字段；v43 新增当前存档专属的 `world.placeHighlights`；v44 新增当前世界 `world.director.preferences`，并分别提供 v42→v43、v43→v44 migration。后续升级仍必须在同一提交提供 migration 与 fixture 测试。
- Provider IndexedDB：v9。
- Content IndexedDB：v12。
- Assets IndexedDB：v3。
- 角色包 schema：v1；世界包 schema：v1；预设包 schema：v2。
- 最近一次完整验证：切片 3 完成为 92 个测试文件、603 项测试通过，`npm run build` 与 `git diff --check` 通过；上一切片为 91 个测试文件、598 项测试通过。
- 当前已知构建提示：主 JavaScript 包约 1.41 MB，Vite 会提示超过 500 kB。它是非阻塞性能项，后续可通过页面级动态导入处理。
- 阶段 10 最后的功能修复为：身份头像设置只在“设置 → 身份”挂载，不再出现在“设置 → 向量”。

开始新工作前仍须以实际 `git status`、测试输出和源码常量为准，不把本文数字当作永远不会变化的事实来源。

---

## 3. 已完成的主要能力

### 世界与剧情

- 确定性时间、地图、日程、相遇、关系、记忆、事件导演、经济与生活循环。
- EventDef、事件包、事件历史、StoryScene 草案/确认/阶段/阅读器。
- 角色包、世界包、事件包导入导出，内置帮助和可下载模板。
- NPC 转正及用户确认式 AI 草稿扩写。
- 地图右上角“地点动态”管理：当前存档内按地点维护热点/活动，支持本地 CRUD、已发现地点标记、导航、节点引用保护，以及经白名单校验后再确认入库的批量/单地点 AI 草稿。活动已支持到场参加、独自/邀请当前在场者、单次叙事调用和用户确认式临时新人；活动流程不写 SaveFile 完成记录或奖励，SaveFile 保持 v43。
- 地图锚点详情内也有“地点动态”面板：只筛选当前 `nodeId`，可单地点生成、手工新建、编辑、删除和参加活动；它与右上角管理抽屉共用同一套 v43 CRUD 和草稿确认流程。右上角抽屉仍保留全图分组、批量生成及未发现地点管理能力。

### 终端与聊天

- 终端桌面新增“剧情导演”：偏好只保存当前世界，首版接入 `narrate_main` / `topic_tree`，不新增 API 调用，不改变事件循环、世界包 v1 或地点动态。
- NPC 偏好已接入现有地点动态生成和活动临时新人候选：复用 `map_activity_gen` / 活动单次 `narrate_main` 请求，空偏好不注入，所有地点与新人白名单、草稿确认和转正边界保持不变。`npc_batch` 当前只处理晨报及既有角色移动，未新增批量新人 UI。
- NPC 偏好也已接入确定性相遇选择：只调整半正式 NPC 的本地权重，采用大小写无关的精确标签匹配与有界增减因子；正式角色、长期未见保证、在场规则、人数上限和固定 seed 行为保持不变。

- 联系人、好友申请、最近会话、引用、贴图、双向转账、重回、远程约定、通话壳、夜间来信。
- 面对面与终端回复恢复、防重复 ops、角色级文本 Provider 绑定。
- 面对面相遇现采用“自动开场一次 → 用户选择话题树 / 自由聊天 / 离开”；话题树不再自动请求，可随时返回选择菜单。
- 相遇选择器支持正式角色与半正式 NPC，并要求多人场景明确主要互动对象。NPC 可使用稳定 ChatRecord 轻量聊天，但所有 ops 都被拒绝，正式关系能力仍需用户确认转正。
- 玩家基础头像和面具头像；终端玩家消息按“当前面具 → 世界玩家 → 名称首字”回退。

### 语音与音乐

- 多个 OpenAI-compatible Speech 配置、全局默认、按世界角色语音绑定。
- 角色消息原位语音附件、指纹缓存、重新生成、下载、统计和安全清理。
- 应用壳层唯一音乐播放器、本地音频、外链音频、显式离线缓存和 Media Session。

### 图像

- 独立图像设置入口、OpenAI-compatible generations / edits、全局画风和角色外貌提示词。
- 按世界隔离的角色锁脸，以及按玩家/面具身份隔离的用户锁脸。
- 设置页生成结果是独立图片，只能预览、下载或删除，不自动替换头像或立绘。
- 面对面角色回复可先编辑 CG 画面草稿，用户确认后再显式生成；CG 是原消息附件，可下载、删除和数据清理。
- 涉及用户的互动 CG 必须满足角色和用户锁脸要求；外链锁脸图不会自动下载或发送给 Provider。

### 设置、主题与数据

- 设置和终端图标桌面、分页与长按排列、标题和图标覆盖。
- 结构化主题外观、实时预览、高级 CSS、CSS 导入导出、独立主题包。
- 全局备份、Provider 设置迁移、可选完整密钥备份及导入预览。
- 资产引用完整性检查、图片/语音/音乐统计和保守清理。
- PWA manifest、PNG 图标、安全区、移动端能力诊断和显式本地完成通知。

### 创意工坊

- 声明式工坊包 v1 已完成本地 ZIP 安装/严格升级闭环、显式版本依赖与生命周期保护、受限 UI 运行时、本地 JSON 编辑器、用户 API 驱动的初稿生成、首个多轮 Agent 闭环、v1 工具协议、能力目录、原子局部 patch、本地工程 inspection、执行层步骤/请求/token 预算、有界自动修复、通用只读 UI 数据绑定，带通用 stats/物品成本和结果文案的 `manual` / `onEnterNode` / `onTimeAdvance` / `onDaySettle` 确定性活动规则，用户显式触发的包内声明式事件，以及工坊包/世界绑定/本地 App 状态/资产的全局备份与只读完整性审计。
- 代码侧收尾验收已使用临时包、Mock Provider 和真实 Dexie/fake-indexeddb 跑通 Agent 创建、安装、运行、更新、分享、卸载、重导入、完整性与全局备份；不发布固定官方示例包。剩余仅是真实用户 Provider 与实际界面的手动体验验证，不是已知功能缺口。
- 包定义、世界绑定和本地 App 状态位于 Content IndexedDB v12；绑定与状态均按 `saveId + packageId` 隔离，图片仍位于 Assets IndexedDB；SaveFile 保持 v41。
- `workshop_draft` 现作为“工坊 Agent”路由：初稿生成仍只发送固定协议与需求；进入编辑器后，每次用户发送先调用一次用户 API，并发送当前工程、最近对话、本地 `capabilities.list` 和 `project.inspect` 结果以便增量修改。inspection 复用编辑器校验，只含状态、诊断、权限和声明式结构摘要，不含正文、世界事实值、已安装包 ID 或二进制。Agent v1 每步只允许一次 `project.replace` 或 `project.patch`；patch 最多 100 项，只支持受限 `add` / `replace` / `remove`，在副本上原子应用。返回结果通过 schema 与正式包完整校验后才替换内存草稿；被拒绝时可在页面预算内自动反馈错误并修复，网络错误不重试，所有失败候选都不进入编辑器。上下文预检前会无损压缩有效工程并按需裁剪旧对话、失败摘录与诊断正文。旧完整工程响应暂时兼容，可撤销上次成功修改，不自动安装。当前活动规则效果仅开放 `add_stat` / `set_flag` / `give_item` / `take_item`；事件本体与 choice 的 ops 由确定性内核原子复核。包内 Prompt 已按预算开放，Provider 文本动作只能由用户按钮显式触发一次，只读取/写回包内本地标量，响应不解析 ops；缺少确定性世界上下文的其他钩子仍禁用。协议说明见 `docs/13-workshop-package-v1.md`。

---

## 4. 不可破坏的数据与安全边界

- AI 只能生成叙述文字和 ops 提议；时间、位置、数值、flags、库存、关系、事件等事实由确定性内核持有。
- 新玩法只能通过注册 op、事件钩子、prompt block 或数据包扩展，不得为单个功能改写核心循环。
- `core/` 不依赖 DOM、IndexedDB、React、Provider 或网络。
- 持久化位于 `data/`；Provider 请求实现位于 `providers/`。
- 默认零 API。只有用户显式点击生成、重生成、连接测试或其他明确联网操作时，才允许单次调用。
- API key、鉴权 headers 和用户数据不得发送到非用户显式配置的端点。
- 二进制只进入 Assets IndexedDB；SaveFile、聊天、包 JSON 只保存 `AssetRef`，禁止写入 base64。
- 外链图片只保存合法 `http(s)` URL，不自动下载；作为锁脸参考时，只有本地二进制才能进入 multipart edits。
- API key 不进入世界包、角色包、事件包、主题包或普通全局备份。
- 新界面必须优先使用语义化 CSS 变量和公共组件，不新增零散硬编码视觉样式。

相关决策优先阅读：

- `docs/04-decisions.md`：D96–D145，创意工坊重点见 D124–D145。
- `docs/03-roadmap.md`：阶段 10。
- `docs/01-architecture.md`：两层划分、ops、事件钩子和 prompt 组装。
- `docs/02-data-model.md`：仅在修改世界 schema 或确定性状态时阅读。

---

## 5. 用户创意工坊的完成状态与验收边界

创意工坊当前规划内的代码实现和自动验收已经完成。目标是让用户调用自己配置的 AI，生成可预览、可安装、可编辑和可分享的终端 App / 玩法包；后续除验收发现的缺陷外，不默认继续扩展新能力。

首要安全结论：不允许 AI 生成并直接执行 JavaScript、TypeScript、React、HTML、任意 CSS 或任意网络请求。AI 只能生成声明式 App 草案，由本地 schema 校验器和受限运行时解释。

推荐包结构：

```text
manifest.json       包 ID、名称、作者、版本、兼容版本和权限
app.json            页面、受限组件、导航、表单和按钮动作
rules.json          声明式条件、活动规则、奖励表和冷却
events.json         可选 EventDef
prompts.json        可选、受预算限制的 prompt block
asset-meta.json     可选资产元数据
assets/*            可选本地二进制资产
```

受限 UI 首版只考虑标题、正文、图片、卡片、列表、标签页、按钮、输入框、选择器、进度和确认框。动作必须来自白名单，例如本地导航、App 草稿状态、读取经授权的世界事实、提交已注册 op、触发已安装事件，以及用户显式触发 Provider。

AI 制作流程应为：

1. 用户填写自然语言需求。
2. 用户显式点击生成，最多调用一次已配置文本 Provider。
3. AI 返回结构化草案，不直接安装、不写世界状态。
4. 本地完成 schema、引用、权限和兼容性校验。
5. 显示 App 预览、权限清单、冲突和不支持能力。
6. 用户确认后安装，终端桌面才出现图标。
7. 导出分享包时排除 API key、Provider 配置、用户存档和游玩状态。

真正的钓鱼、种田、开店结果必须由确定性活动引擎计算；AI 只描述已经确认的结果。某项需求若没有已注册通用动作，工坊必须显示“不支持”，不能退化为任意代码或让 AI 直接改状态。

### 推荐垂直切片

1. **已完成**：定义 App 包 schema、权限枚举、校验报告、导入导出和本地安装/卸载；不接 AI，不渲染动态 UI。
2. **已完成**：实现受限 UI 渲染器、终端动态图标和只读/本地状态组件。
3. **已完成**：实现工坊编辑器、实时预览、权限与冲突展示。
4. **已完成**：接入用户 API 显式触发的初稿生成；结果必须先校验和预览。
5. **已完成首个子切片**：注册通用确定性活动 op，并接入 `manual` / `onEnterNode` 钩子、安全条件、冷却、一次性与原子效果。
6. **已完成首个子切片**：工坊 Agent 可读取当前工程、最近对话和校验诊断，通过用户 API 进行多轮完整工程修改，并支持撤销上次 Agent 修改。
7. **已完成协议骨架**：Agent v1 使用版本化严格工具协议，当前每轮只允许一次完整工程 `project.replace`；旧响应暂时兼容。
8. **已完成**：只读 `capabilities.list` 在每轮请求前纯本地查询，并入同一次 API 请求；目录不读取用户或世界数据。
9. **已完成**：`project.patch` 支持有上限的局部 `add` / `replace` / `remove`，整组在工程副本上原子应用并重新过 schema。
10. **已完成**：只读 `project.inspect` 复用编辑器校验，并提供不含正文或世界事实值的结构化预览摘要。
11. **已完成预算子切片**：编辑器可配置步骤、请求、单次/总输出 token 和安全余量；执行层在联网前检查上下文并收紧每步适配器输出参数。预算不持久化，不虚构货币价格。
12. **已完成**：在现有预算执行契约上实现有上限的自动修复循环；只修复本地拒绝结果，不重试网络错误，失败候选不进入编辑器。
13. **已完成首个子切片**：统一 UI 数据绑定可从包内本地标量或授权世界安全快照读取显示值，覆盖标题、正文、卡片、列表、按钮/确认文案和进度；不执行模板或表达式。
14. **已完成成本/结果子切片**：活动可声明通用 stats/物品成本和成功/失败文案；成本先聚合检查，再与效果原子提交，不消耗时间或行动点。
15. **已完成安全子集**：自动活动已扩展到携带确定性世界上下文的 `onTimeAdvance` / `onDaySettle`；不修改核心 payload，也不接入会递归或只服务 prompt 的钩子。
16. **已完成事件子切片**：启用包的事件定义按来源同步到当前世界；同包 `trigger-event` 按钮可显式触发，事件本体和 choice 只组合能力目录公开的原子安全 ops，不进入导演随机池、不调用 API。
17. **已完成**：开放有预算的包内 prompt block 注册；仅接入现有 PromptAssembler 的 `narrate_main` / `topic_tree`，不增加 API 次数，也未开放 Provider 动作。
18. **已完成**：开放用户显式 Provider 文本动作；每次点击最多一次用户 API，输入/结果只走包内本地状态，响应不解析 ops，也不发送完整世界事实。
19. **已完成包更新子切片**：同 ID 包只允许显式升级到严格更高版本；确认前显示权限与身份变化，更新保留全部世界绑定、enabled、本地 App 状态和旧资产，失败时补偿本轮新资产。
20. **已完成**：版本依赖支持包含式最低版本和排除式最高版本；安装、更新、启停、卸载均复核全局版本与世界启用关系，拒绝依赖环且不自动联网或安装依赖。
21. **已完成**：把工坊包、世界绑定、本地 App 状态和资产纳入全局备份 v5，并提供当前数据与导入预览的只读完整性审计。
22. **已完成代码侧验收**：使用不随产品发布的临时包、Mock Provider 与真实 Dexie 数据访问路径（由 fake-indexeddb 提供测试环境）验证 Agent 制作、安装、运行、更新、分享、卸载和重导入；不制作固定官方示例包。

真实 API 最短手测仍由用户完成：选择自己的 `workshop_draft` Provider，用自然语言生成一个 App，继续对话修改，安装并写入本地 App 状态，要求 Agent 提高版本后更新，再导出 ZIP 并重新导入。API key 只留在用户浏览器；测试不连接真实端点。若手测发现问题，按单独缺陷切片处理。

创意工坊原始想法见 `docs/ideas.md` 的“用户创意工坊 / 玩法包”。正式编码前应先形成独立计划并确认包状态放置、权限模型、跨世界绑定和卸载语义。

---

## 6. 已延期或保留的候选

- Animal Island 风格内置可切换皮肤。
- Tauri 原生构建、安装包和受权限约束的桌面 Provider 传输。
- 网易云适配。
- 音频辅助保活。
- 更进一步的移动端通知与后台任务。
- 快照回退后的聊天分支。
- 玩家衣柜、角色库存、多货币、动态物品等可能需要世界 schema 的系统。
- 地点动态管理与活动参加切片均已完成；下一垂直切片需按新计划确认后再立项（例如活动更丰富的参与后续、衣柜、货币编辑或用户教程），不得在本切片顺手扩展。
- 主包动态拆分和加载性能优化。

这些候选不得在创意工坊切片中顺手实现；需要分别立项。

---

## 7. 验证与提交纪律

每个垂直切片完成后必须运行：

```text
npm run build
npm test -- --run
git diff --check
```

每个切片创建一个本地 commit，不 push；提交后停止并报告剩余工作顺序。界面实际手测由用户负责，代码侧只做短时 smoke、自动测试和边界验证。真实 Provider、Android/iOS、PWA standalone 与 Media Session 真机能力不能由桌面自动测试替代。
